This is the Davisbase Database Engine.

To run the project please download the DavisbaseEngine.jar file and run it from command line.

The project has been exceuted according to the given design document.

This database supports the following commands:
	SHOW TABLES
	HELP
	INSERT 
	CREATE
	SELECT
	SELECT .. WHERE
	DROP
	VERSION

